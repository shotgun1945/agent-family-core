[project]
name = "family-wiki-mcp"
version = "0.1.0"
description = "MCP server exposing the Family Core wiki query tools"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[tool.uv]
package = false
